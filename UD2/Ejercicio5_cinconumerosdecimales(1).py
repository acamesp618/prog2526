lista=[0]*5 #se crea una lista con 5 espacios con 0
for i in range (5): #se añade 5 porque es el numero de veces que queremos añadir valores
    lista[i]=float(input("Escribe un numero decimal: ")) #se modifican los valores del array
print(lista)
